const runSemanticMatch = async (req, res) => {
  // Placeholder: run vector/semantic task matching for volunteer allocation.
  res.status(501).json({
    message: "Volunteer semantic match service is not implemented yet.",
  });
};

module.exports = {
  runSemanticMatch,
};
